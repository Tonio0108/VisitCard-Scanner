name: visit_card_scanner
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
    sdk: ^3.8.1

dependencies:
    flutter:
        sdk: flutter
    image_picker: ^1.0.0
    path: ^1.9.1
    sqflite: ^2.4.2
    url_launcher: ^6.2.5
    path_provider: ^2.0.11
    google_mlkit_text_recognition: ^0.12.0
    flutter_contacts: ^1.1.7+1
    permission_handler: ^11.0.1
    http: ^1.4.0
    connectivity_plus: ^5.0.0
    flutter_dotenv: ^5.1.0
dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^5.0.0

flutter:
    uses-material-design: true
